---
description: Launch the PDF editor GUI
---

Launch the PDF editor GUI with `.venv/bin/python -m app` (equivalent: activate the venv first with `source .venv/bin/activate`, then `python -m app`).

The GUI requires a display. If none is available, tell the user and suggest running the headless smoke test via `/test` instead.